name: Property App Ci/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: SSH to Server and Clone Repo
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: 72.61.229.5
          username: root
          password: Tuccimar@1230
          script: |
            cd /home/ubuntu/
            sudo ./update.sh
