name: Property App Ci/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: SSH to Server and Clone Repo
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: 187.77.189.57
          username: root
          password: Application@12390
          script: |
            cd /home/ourlisting/
            sudo ./deploy.sh
